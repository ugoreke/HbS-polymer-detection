"""Minimal sickling subset bundled with the demo so this folder is
path-independent. Lifted verbatim from the project's main ``sickling/`` package,
with ``config.py`` slimmed down to the demo's needs (no YAML loader)."""
